const Duration kAnimationDuration = Duration(milliseconds: 1200);
const Duration kCardSwipeDuration = Duration(milliseconds: 600);
const kNavigationDuration = Duration(milliseconds: 800);
const kToastAnimationDuration = Duration(milliseconds: 1200);
const kToastCloseDuration = Duration(seconds: 3);
const kImageHoverDuration = Duration(milliseconds: 400);
const kPopUpMenuDuration = Duration(milliseconds: 600);
